import requests
import numpy as np

# Observation = [외기온, 습도, 에어컨 온도, 풍량, 에어컨 위치(x,y)] + 승객 10명 * [x, y, enter_time, vote]
obs = [
    32.5,   # 외기온 (°C)
    60.0,   # 외기습도 (%)
    24.0,   # 에어컨 온도 설정 (°C)
    2.0,    # 에어컨 풍량 설정 (단계 0~4)
    0.0, 0.0,  # 에어컨 위치 (x, y)

    # 승객 10명 * [x, y, enter_time, vote]
    -3.0, 0.5, 2.0,  0,
     1.5, -0.2, 3.0, 1,
    -6.5, 0.1, 4.5, -1,
     2.2, -0.8, 1.0, 0,
    -1.0, 0.0, 0.5, -2,
     3.3, 0.9, 2.2, 2,
    -2.5, -0.5, 1.5, 0,
     0.5, 0.7, 2.8, 1,
    -4.4, -0.9, 3.3, -1,
     1.8, 0.3, 1.9, 0
]

res = requests.post("http://127.0.0.1:8000/predict", json={"state": obs})
print("서버 응답:", res.json())
